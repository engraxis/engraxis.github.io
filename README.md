
# Go to my personal webpage
https://engraxis.github.io

# To run the js scripts
1. On VS Code, install the extention "Live Server".
2. In the parent directory, run on terminal: python3 -m http.server 8000
3. No loading the page will run the "fetch()" function inside js scripts correctly.
